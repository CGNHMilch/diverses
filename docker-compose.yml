---
version: '3'

services:
  peer2profit:
    image: peer2profit/peer2profit_linux:latest
    container_name: peer2profit
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    environment:
      - P2P_EMAIL=handymilch@gmail.com
    network_mode: "bridge"